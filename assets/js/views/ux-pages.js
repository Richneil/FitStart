import { goals } from "../data/demo-profile.js";
import { scoreAssessment } from "../services/priority-service.js";
import { explainMetric } from "../services/explanation-service.js";
import { metricLibrary, getMetricDefinition } from "../data/metric-library.js";
import { escapeHtml, label, formatDate, badge, pageHeader, notice } from "../components/ui.js";
import { radioGroup } from "../components/forms.js";

const currentAssessment = state => state.assessments.at(-1);
const resultFor = state => scoreAssessment(currentAssessment(state), state.profile);
const cleanStatus = value => String(value || "").replace(/^Demo:\s*/i, "");

const safetyNote = () => `<div class="safety-strip" role="note"><span class="safety-icon" aria-hidden="true">i</span><p><strong>Use this as a guide.</strong> FITSTART explains assessment measurements in simpler language. It does not diagnose a health condition or replace advice from a qualified fitness professional.</p></div>`;

const journeySteps = (active = 1) => `<ol class="journey-steps" aria-label="Your FITSTART progress">
  <li class="${active >= 1 ? "is-done" : ""}"><span>1</span><div><strong>Your profile</strong><small>Goal and experience</small></div></li>
  <li class="${active >= 2 ? "is-done" : ""}"><span>2</span><div><strong>Your assessment</strong><small>Add and check the report</small></div></li>
  <li class="${active >= 3 ? "is-done" : ""}"><span>3</span><div><strong>Your results</strong><small>See what to understand first</small></div></li>
</ol>`;

export function welcomeView() {
  return `<div class="welcome-shell ux-welcome"><nav class="welcome-nav"><a class="brand" href="#/welcome"><span class="brand-mark">FS</span><span>FITSTART</span></a><button class="help-link" data-action="open-boundaries"><span aria-hidden="true">?</span> What is FITSTART?</button></nav><main class="welcome-main" id="main-content"><section class="welcome-copy"><span class="welcome-pill">Made for new gym members</span><h1>Understand your body assessment without the confusing terms.</h1><p class="lead">FITSTART organizes your report and shows the first result worth understanding based on your goal. Your original numbers always stay visible.</p><div class="welcome-actions"><a class="button button-primary button-large" href="#/profile/setup">Get started <span aria-hidden="true">→</span></a></div><p class="privacy-reassurance"><span aria-hidden="true">⌂</span> Your test information stays in this browser and can be cleared anytime.</p></section><aside class="welcome-visual" aria-label="What FITSTART helps you do"><div class="focus-preview friendly-preview outcome-preview"><span class="badge badge-primary">What you’ll get</span><h2 class="spacer-top">A clear place to begin</h2><ul class="outcome-list"><li><span>1</span><div><strong>One result highlighted first</strong><p>So you are not expected to understand every number at once.</p></div></li><li><span>2</span><div><strong>A simple explanation</strong><p>Every term is translated into everyday language.</p></div></li><li><span>3</span><div><strong>A clear reason</strong><p>See how your goal and report influenced the order.</p></div></li></ul></div></aside></main><section class="welcome-how"><p class="eyebrow">Your three steps</p>${journeySteps(0)}</section></div>`;
}

export function profileSetupView(state) {
  return `<div class="content ux-content"><div class="setup-header"><div><a class="back-link" href="#/welcome">← Back</a><p class="eyebrow spacer-top">Step 1 of 3</p><h1>Tell us what matters to you</h1><p>These three answers help FITSTART choose the right level of detail and organize your results.</p></div>${journeySteps(1)}</div><div class="form-focus-layout"><form class="card profile-form ux-form" id="profile-setup-form" novalidate><section class="form-section"><div class="question-number">1</div><div class="field"><label for="displayName">What should we call you?</label><p class="field-help">A first name or nickname is enough.</p><input class="input input-large" id="displayName" name="displayName" value="${escapeHtml(state.profile?.displayName || "")}" autocomplete="nickname" placeholder="For example, Alex" required><p class="field-error" data-error="displayName" aria-live="polite"></p></div></section><section class="form-section"><div class="question-number">2</div><fieldset class="field"><legend>What is your main fitness goal?</legend><p class="field-help">Choose the goal that matters most to you right now.</p>${radioGroup("primaryGoal", goals, state.profile?.primaryGoal || "")}<p class="field-error" data-error="primaryGoal" aria-live="polite"></p></fieldset></section><section class="form-section"><div class="question-number">3</div><fieldset class="field"><legend>How familiar are you with body assessments?</legend><p class="field-help">This changes how much detail we show, not your measurements.</p>${radioGroup("experience", ["new", "some-experience", "experienced"], state.profile?.experience || "")}<p class="field-error" data-error="experience" aria-live="polite"></p></fieldset></section><input type="hidden" name="secondaryGoal" value=""><div class="form-actions"><a class="button button-secondary" href="#/welcome">Back</a><button class="button button-primary" type="submit">Save and continue <span aria-hidden="true">→</span></button></div></form><aside class="reassurance-panel"><div class="reassurance-icon" aria-hidden="true">✓</div><h2>Only the essentials</h2><ul class="friendly-list"><li>No medical-history questions</li><li>No account is required for testing</li><li>You can change these answers later</li><li>Your entries stay on this device</li></ul></aside></div></div>`;
}

export function dashboardView(state) {
  const assessment = currentAssessment(state);
  const name = escapeHtml(state.profile?.displayName || "Member");
  if (!assessment) {
    return `<div class="content ux-content"><header class="friendly-header"><div><p class="eyebrow">Home</p><h1>Hi ${name}, your profile is ready.</h1><p>Add your assessment report so FITSTART can organize and explain the measurements.</p></div><span class="progress-badge">Step 1 complete</span></header>${journeySteps(1)}<section class="next-action-card"><div class="next-action-icon" aria-hidden="true">2</div><div><p class="eyebrow">Next step</p><h2>Add your assessment</h2><p>Upload a report screenshot or use its QR code. You will check every captured value before results are created.</p></div><a class="button button-primary button-large" href="#/assessment/import">Add my assessment →</a></section><div class="two-columns results-section"><div class="card quiet-card"><p class="eyebrow">Not sure about a term?</p><h3>Use the simple glossary</h3><p>Learn what common assessment measurements mean.</p><a class="text-link" href="#/learn">Open the glossary →</a></div><div class="card profile-mini"><span class="avatar" aria-hidden="true">${name.slice(0, 1).toUpperCase()}</span><div><p class="eyebrow">Your main goal</p><h3>${label(state.profile?.primaryGoal)}</h3><a class="text-link" href="#/profile">Edit profile</a></div></div></div>${safetyNote()}</div>`;
  }
  const result = resultFor(state);
  const main = result.mainFocus;
  const explanation = main ? explainMetric(main, state.profile) : null;
  return `<div class="content ux-content"><header class="friendly-header"><div><p class="eyebrow">Home</p><h1>Welcome back, ${name}.</h1><p>Your latest assessment is ready. Start with the result highlighted below.</p></div><a class="button button-secondary" href="#/profile">Edit profile</a></header><section class="dashboard-focus"><div class="focus-kicker"><span class="badge badge-primary">Start here</span><span>Assessment · ${formatDate(assessment.assessedAt)}</span></div><div class="dashboard-focus-grid"><div><p class="eyebrow">First result to understand</p><h2>${escapeHtml(main?.name || "Review your assessment")}</h2><p class="focus-summary">${escapeHtml(explanation?.meaning || "Check your assessment values to continue.")}</p><div class="why-inline"><span aria-hidden="true">💡</span><p>${escapeHtml(explanation?.why || "Your goal and report are used to organize the results.")}</p></div><a class="button button-primary button-large" href="#/results">View my results →</a></div>${main ? `<div class="value-panel"><p>Your report value</p><strong>${main.value}</strong><span>${escapeHtml(main.unit)}</span>${badge(main)}<small>This number is kept exactly as confirmed.</small></div>` : ""}</div></section><section class="dashboard-actions"><article class="mini-action"><span class="mini-action-icon" aria-hidden="true">＋</span><div><h3>Add a new assessment</h3><p>Use a newer report.</p></div><a href="#/assessment/import" aria-label="Add a new assessment">→</a></article><article class="mini-action ${state.assessments.length < 2 ? "is-muted" : ""}"><span class="mini-action-icon" aria-hidden="true">↔</span><div><h3>Compare reports</h3><p>${state.assessments.length < 2 ? "Available after a second report." : "See what measurements changed."}</p></div>${state.assessments.length < 2 ? `<span aria-hidden="true">—</span>` : `<a href="#/compare" aria-label="Compare reports">→</a>`}</article><article class="mini-action"><span class="mini-action-icon" aria-hidden="true">?</span><div><h3>Learn a term</h3><p>Read simple definitions.</p></div><a href="#/learn" aria-label="Open the glossary">→</a></article></section>${safetyNote()}</div>`;
}

export function assessmentImportView() {
  return `<div class="content ux-content"><div class="setup-header compact"><div><a class="back-link" href="#/dashboard">← Back to Home</a><p class="eyebrow spacer-top">Step 2 of 3</p><h1>Add your assessment report</h1><p>Choose the option that matches what you have. You will review all values before FITSTART creates your results.</p></div>${journeySteps(2)}</div><section class="capture-choice-grid"><article class="recommended-method upload-method"><div><span class="recommend-label">Recommended</span><div class="method-heading"><span class="method-icon" aria-hidden="true">▣</span><div><h2>Upload a report screenshot</h2><p>Select a clear image of your assessment report.</p></div></div><div class="field spacer-top"><label for="report-upload">Choose an image</label><input class="input" id="report-upload" type="file" accept="image/*"><p class="field-help">The image is used only during this test and is not saved in browser storage.</p></div></div><button class="button button-primary button-large" data-action="simulate-screenshot">Continue with screenshot →</button></article><article class="card method-card qr-method"><div class="method-icon" aria-hidden="true">⌗</div><h2>Use a QR result</h2><p>Choose this if your assessment report includes a QR code.</p><div class="qr-placeholder" aria-hidden="true"><span>⌗</span><small>QR reader preview</small></div><button class="button button-secondary" data-action="simulate-qr">Continue with QR →</button></article></section>${notice("Report reading and QR scanning are simulated in this test version. FITSTART will prefill test values for you to review and correct before continuing.", "warning", "About this test version")}</div>`;
}

export function assessmentReviewView(state) {
  const assessment = state.draftAssessment;
  if (!assessment) return `<div class="content"><div class="card empty-state"><div class="empty-icon" aria-hidden="true">i</div><h1>No assessment to review</h1><p>Add a report first.</p><a class="button button-primary spacer-top" href="#/assessment/import">Add assessment</a></div></div>`;
  const previous = currentAssessment(state);
  const sourceText = assessment.sourceType === "qr" ? "QR result" : escapeHtml(assessment.fileName || "Uploaded screenshot");
  return `<div class="content ux-content"><div class="setup-header compact"><div><a class="back-link" href="#/assessment/import">← Change report</a><p class="eyebrow spacer-top">Check before continuing</p><h1>Do these values match your report?</h1><p>Correct anything that looks different. FITSTART will not create results until you confirm this screen.</p></div><div class="review-status"><span aria-hidden="true">7</span><p><strong>measurements</strong><br><small>${sourceText}</small></p></div></div>${notice("The values were prefilled for this test. Compare them carefully with your own report and edit them when needed.", "warning", "Please check every value")}<form id="assessment-review-form" novalidate><div class="review-single"><div class="metric-review-list">${assessment.metrics.map((metric, index) => `<article class="metric-review-card"><div class="metric-review-heading"><span class="metric-number">${index + 1}</span><div><h2>${escapeHtml(metric.name)}</h2><p class="field-error" data-error="${metric.id}" aria-live="polite"></p></div><span class="check-hint">Check</span></div><div class="metric-review-controls"><div class="field value-field"><label for="metric-${metric.id}">Value on your report</label><div class="unit-input"><input class="input" id="metric-${metric.id}" name="metric-${metric.id}" type="number" step="any" value="${metric.value === null ? "" : escapeHtml(metric.value)}" ${metric.value === null ? "disabled" : ""}><span>${escapeHtml(metric.unit)}</span></div></div><div class="field category-field"><label for="category-${metric.id}">Label shown on your report</label><select class="select" id="category-${metric.id}" name="category-${metric.id}"><option value="within" ${metric.sourceCategory === "within" ? "selected" : ""}>Within expected range</option><option value="attention" ${metric.sourceCategory === "attention" ? "selected" : ""}>Worth reviewing</option><option value="higher-attention" ${metric.sourceCategory === "higher-attention" ? "selected" : ""}>Review first</option></select></div></div><div class="metric-review-actions"><button class="text-link" type="button" data-action="toggle-unavailable" data-id="${metric.id}">${metric.value === null ? "Restore this measurement" : "This measurement is not on my report"}</button><button class="text-link" type="button" data-action="reset-metric" data-id="${metric.id}">Restore captured value</button></div></article>`).join("")}</div>${previous ? notice("After you confirm, you can compare this report with your previous one.", "warning", "Previous report found") : ""}<label class="confirmation-box"><input type="checkbox" name="reviewed" required><span><strong>I checked the values and report labels.</strong><small>They now match the assessment report I am using for this test.</small></span></label><p class="field-error" data-error="reviewed" aria-live="polite"></p><div class="sticky-actions"><a class="button button-secondary" href="#/assessment/import">Go back</a><button class="button button-primary button-large" type="submit">Create my results →</button></div></div></form></div>`;
}

export function resultsView(state) {
  const assessment = currentAssessment(state);
  if (!assessment) return `<div class="content"><div class="card empty-state"><h1>No results yet</h1><p>Add and confirm an assessment first.</p><a class="button button-primary spacer-top" href="#/assessment/import">Add assessment</a></div></div>`;
  const result = resultFor(state);
  if (result.insufficient) return `<div class="content">${pageHeader("Your results", "We need a few more values", "FITSTART could not organize the report using the available measurements.")}${notice("Missing measurements are left out. They are never counted as zero.", "warning", "Check your assessment")}<a class="button button-primary" href="#/assessment/import">Add the report again</a></div>`;
  const main = result.mainFocus;
  const explanation = explainMetric(main, state.profile);
  const memberName = escapeHtml(state.profile?.displayName || "Member");
  return `<div class="content ux-content"><header class="result-header"><div><a class="back-link" href="#/dashboard">← Back to Home</a><p class="eyebrow spacer-top">${memberName}’s results</p><h1>Your assessment, made easier to understand.</h1><p>Report added ${formatDate(assessment.assessedAt)} · Your confirmed values have not been changed.</p></div></header>${safetyNote()}<div class="result-map" aria-label="How to read this page"><span class="is-current"><b>1</b> Start here</span><span><b>2</b> Why it came first</span><span><b>3</b> Other results</span></div><article class="card main-focus ux-main-focus"><div class="main-focus-grid"><div><span class="badge">First result to understand</span><p class="eyebrow spacer-top">Start with this one</p><h2>${escapeHtml(main.name)}</h2><div class="plain-explanation light"><span aria-hidden="true">💡</span><p><strong>What it means</strong><br>${escapeHtml(explanation.meaning)}</p></div><a class="button button-light button-large spacer-top" href="#/results/metric/${main.id}">Read the full explanation →</a></div><div class="value-panel dark"><p>Your report value</p><strong>${main.value}</strong><span>${escapeHtml(main.unit)}</span>${badge(main)}<small>This number is unchanged.</small></div></div></article><section class="results-section"><div class="section-heading numbered-heading"><span>2</span><div><p class="eyebrow">Why this came first</p><h2>Your goal and report pointed to it</h2><p>These are the two reasons used to organize your results.</p></div></div><div class="why-grid friendly-why"><div class="why-callout"><span class="callout-icon" aria-hidden="true">◎</span><strong>Your goal</strong><p>${escapeHtml(explanation.toldUs)}</p></div><div class="why-callout"><span class="callout-icon" aria-hidden="true">▤</span><strong>Your report</strong><p>${escapeHtml(explanation.showed)}</p></div></div></section><section class="results-section"><div class="section-heading numbered-heading"><span>3</span><div><p class="eyebrow">Other results</p><h2>Understand these next</h2><p>You can explore these after you understand the first result.</p></div></div><div class="priority-list friendly-priorities">${result.priorities.map((item, index) => { const itemExplanation = explainMetric(item, state.profile); return `<article class="card priority-card"><span class="priority-rank">${index + 2}</span><div><p class="supporting">Next result ${index + 1}</p><h3>${escapeHtml(item.name)}</h3><p class="priority-value">${item.value} ${escapeHtml(item.unit)} <span>· ${escapeHtml(cleanStatus(item.sourceCategoryLabel))}</span></p><p class="supporting">${escapeHtml(itemExplanation.meaning)}</p></div><a class="button button-secondary" href="#/results/metric/${item.id}">Explain this</a></article>`; }).join("")}</div></section><details class="more-section results-section"><summary><span><strong>See more about this assessment</strong><small>Simple glossary and all confirmed report values</small></span><span aria-hidden="true">＋</span></summary><div class="more-section-body"><div class="two-columns"><div class="card card-soft"><p class="eyebrow">Simple glossary</p><h2>Look up an unfamiliar term</h2><p>Read short definitions for every measurement.</p><a class="button button-soft spacer-top" href="#/learn">Open the glossary</a></div><div class="card original-report-card"><div><p class="eyebrow">Your complete report</p><h2>See every confirmed value</h2><p>Nothing is hidden by the organized results.</p></div><button class="button button-secondary" data-action="view-full-assessment">View all values</button></div></div></div></details><div class="professional-banner"><span aria-hidden="true">i</span><div><strong>This is an explanation, not a diagnosis.</strong><p>Discuss your assessment with a qualified fitness professional before making decisions based on a measurement.</p></div></div></div>`;
}

export function metricDetailView(state, id) {
  const definition = getMetricDefinition(id);
  if (!definition) return `<div class="content"><div class="card empty-state"><h1>Measurement not found</h1><a class="button button-primary spacer-top" href="#/results">Back to results</a></div></div>`;
  const result = currentAssessment(state) ? resultFor(state) : null;
  const scored = result?.items?.find(item => item.id === id);
  if (!scored) return learnMetricView(state, id);
  const explanation = explainMetric(scored, state.profile);
  const position = [result.mainFocus, ...result.priorities].findIndex(item => item?.id === id) + 1;
  return `<div class="content ux-content"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="#/results">My Results</a><span>/</span><span>${escapeHtml(scored.name)}</span></nav>${pageHeader("Simple explanation", scored.name, "What this measurement means and why it appears in your results.")}<div class="content-grid"><section class="stack-lg"><div class="card"><div class="card-header"><div>${badge(scored)}<p class="eyebrow spacer-top">Your report value</p></div><div class="metric-value"><strong>${scored.value}</strong><span>${escapeHtml(scored.unit)}</span></div></div><h2 class="spacer-top">In simple terms</h2><p>${escapeHtml(definition.definition)}</p><p class="supporting spacer-top">${escapeHtml(definition.limitation)}</p></div><div class="why-grid friendly-why"><div class="why-callout"><span class="callout-icon" aria-hidden="true">◎</span><strong>Your goal</strong><p>${escapeHtml(explanation.toldUs)}</p></div><div class="why-callout"><span class="callout-icon" aria-hidden="true">▤</span><strong>Your report</strong><p>${escapeHtml(explanation.showed)}</p></div></div><div class="card"><h2>Why it appears here</h2><p>${escapeHtml(explanation.why)}</p><details class="accordion spacer-top"><summary>See how FITSTART organized it</summary><div class="accordion-body"><dl class="summary-list"><div class="summary-row"><dt>Report label</dt><dd>${escapeHtml(cleanStatus(scored.sourceCategoryLabel))}</dd></div><div class="summary-row"><dt>Goal connection</dt><dd>${scored.primaryMatch ? "Directly connected" : "Connected through another result"}</dd></div><div class="summary-row"><dt>Position in your results</dt><dd>${position > 0 ? `Number ${position}` : "Additional information"}</dd></div></dl></div></details></div></section><aside class="stack-lg"><div class="card card-soft"><p class="eyebrow">Related terms</p>${definition.related.map(relatedId => { const metric = getMetricDefinition(relatedId); return `<a class="text-link" href="#/learn/${metric.id}">${escapeHtml(metric.name)} →</a>`; }).join("<br>")}</div>${notice("Discuss this measurement with a qualified fitness professional before making decisions based on it.", "warning", "Keep this in context")}</aside></div></div>`;
}

export function learnView(state) {
  const recent = state.ui.recentMetrics || [];
  return `<div class="content ux-content">${pageHeader("Simple glossary", "Understand the words on your report", "Choose any measurement for a short explanation in everyday language.")}<div class="search-row"><label class="sr-only" for="metric-search">Search measurements</label><input class="input" type="search" id="metric-search" placeholder="Search measurements…"><button class="button button-secondary" data-action="clear-search">Clear</button></div><div class="filter-row spacer-top" aria-label="Measurement groups"><button class="filter is-active" data-filter="all">All</button>${[...new Set(metricLibrary.map(metric => metric.category))].map(category => `<button class="filter" data-filter="${category}">${category}</button>`).join("")}</div>${recent.length ? `<p class="supporting spacer-top">Recently viewed: ${recent.map(id => `<a href="#/learn/${id}">${escapeHtml(getMetricDefinition(id)?.name || id)}</a>`).join(" · ")}</p>` : ""}<section class="three-columns results-section" id="library-grid">${metricLibrary.slice().sort((a, b) => a.name.localeCompare(b.name)).map(metric => `<article class="card library-card" data-library-card data-name="${escapeHtml((metric.name + " " + metric.abbr).toLowerCase())}" data-category="${metric.category}"><span class="badge badge-neutral">${escapeHtml(metric.category)}</span><h2 class="spacer-top">${escapeHtml(metric.name)}</h2><p class="supporting">${escapeHtml(metric.definition)}</p><a class="text-link" href="#/learn/${metric.id}">Read simple explanation →</a></article>`).join("")}</section><div class="card empty-state" id="library-empty" hidden><div class="empty-icon" aria-hidden="true">?</div><h2>No matching measurement</h2><p>Try a shorter search.</p></div></div>`;
}

export function learnMetricView(state, id) {
  const metric = getMetricDefinition(id);
  if (!metric) return `<div class="content"><div class="card empty-state"><h1>Measurement not found</h1><a class="button button-primary spacer-top" href="#/learn">Back to glossary</a></div></div>`;
  const assessmentMetric = currentAssessment(state)?.metrics.find(item => item.id === id);
  return `<div class="content ux-content"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="#/learn">Simple Glossary</a><span>/</span><span>${escapeHtml(metric.name)}</span></nav>${pageHeader(metric.category, metric.name, metric.abbr !== "—" ? `You may also see “${metric.abbr}” on a report.` : "A simple guide to this measurement.")}<div class="content-grid"><article class="stack-lg"><section class="card"><p class="eyebrow">In simple terms</p><h2>${escapeHtml(metric.definition)}</h2></section><section class="card"><h2>What it shows</h2><p>${escapeHtml(metric.represents)}</p><h3 class="spacer-top">Why people look at it</h3><p>${escapeHtml(metric.relevance)}</p></section><section class="notice notice-warning"><span class="notice-icon" aria-hidden="true">!</span><div><strong>Keep in mind</strong><p>${escapeHtml(metric.limitation)}</p></div></section></article><aside class="stack-lg">${assessmentMetric ? `<div class="card card-soft"><p class="eyebrow">Your report</p><h3>${assessmentMetric.value} ${escapeHtml(assessmentMetric.unit)}</h3>${badge(assessmentMetric)}<a class="button button-soft button-block spacer-top" href="#/results/metric/${id}">Back to my result</a></div>` : ""}<div class="card"><p class="eyebrow">Related terms</p>${metric.related.map(relatedId => { const related = getMetricDefinition(relatedId); return `<a class="text-link" href="#/learn/${related.id}">${escapeHtml(related.name)} →</a>`; }).join("<br>")}</div></aside></div></div>`;
}

export function profileView(state) {
  const profile = state.profile;
  if (!profile) return `<div class="content"><div class="card empty-state"><h1>No profile yet</h1><p>Create your profile to begin.</p><a class="button button-primary spacer-top" href="#/profile/setup">Create profile</a></div></div>`;
  return `<div class="content ux-content">${pageHeader("Your profile", "Your fitness context", "Update the three answers FITSTART uses to organize and explain your report.")}<div class="content-grid"><form class="card profile-form" id="profile-update-form"><div class="field"><label for="displayName">Name or nickname</label><input class="input" id="displayName" name="displayName" value="${escapeHtml(profile.displayName)}"></div><fieldset class="field"><legend>Main fitness goal</legend>${radioGroup("primaryGoal", goals, profile.primaryGoal)}</fieldset><fieldset class="field"><legend>Experience with body assessments</legend>${radioGroup("experience", ["new", "some-experience", "experienced"], profile.experience)}</fieldset><input type="hidden" name="secondaryGoal" value=""><p class="field-error" data-error="profile" aria-live="polite"></p><button class="button button-primary" type="submit">Save changes</button></form><aside class="stack-lg"><div class="card card-soft"><p class="eyebrow">Your privacy</p><h2>Saved only in this browser</h2><p>Your profile and up to two confirmed assessments stay on this device for testing.</p><button class="button button-danger spacer-top" data-action="clear-data">Clear all saved data</button></div>${safetyNote()}</aside></div></div>`;
}
