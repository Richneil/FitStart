import { demoProfile, goals, activities, barriers, expectations } from "../data/demo-profile.js";
import { demoAssessments, blankMetrics } from "../data/demo-assessments.js";
import { metricLibrary, getMetricDefinition } from "../data/metric-library.js";
import { rules } from "../data/rules.js";
import { scoreAssessment } from "../services/priority-service.js";
import { explainMetric, quickWins } from "../services/explanation-service.js";
import { compareAssessments } from "../services/comparison-service.js";
import { escapeHtml, label, formatDate, badge, pageHeader, notice } from "../components/ui.js";
import { radioGroup, chips, selectOptions } from "../components/forms.js";

const educationalNotice=()=>notice("Fictional demonstration data only. FITSTART explains a report; it does not diagnose, treat, or replace qualified professional guidance.");
const currentAssessment=state=>state.assessments.at(-1);
const resultFor=state=>scoreAssessment(currentAssessment(state),state.profile);

export function welcomeView(){return `<div class="welcome-shell"><nav class="welcome-nav"><a class="brand" href="#/welcome"><span class="brand-mark">FS</span><span>FITSTART</span></a><button class="text-link" data-action="open-boundaries">What FITSTART does not do</button></nav><main class="welcome-main" id="main-content"><section class="welcome-copy"><p class="eyebrow">Your assessment, made understandable</p><h1>Understand your assessment. Start with what matters to you.</h1><p class="lead">FITSTART keeps every original FitMao value visible, then adds a transparent educational layer shaped by your goal, experience, and starting context.</p><div class="welcome-steps"><div class="welcome-step"><span class="welcome-step-number">1</span><div><strong>Add Assessment</strong><p class="supporting">Use fictional demo values or review values you enter.</p></div></div><div class="welcome-step"><span class="welcome-step-number">2</span><div><strong>Tell Us About You</strong><p class="supporting">Share only the context needed for clearer explanations.</p></div></div><div class="welcome-step"><span class="welcome-step-number">3</span><div><strong>Understand Your Starting Point</strong><p class="supporting">See one Main Focus, the reasons behind it, and useful questions to explore.</p></div></div></div><div class="cluster"><a class="button button-primary" href="#/profile/setup">Create My Starting Point <span aria-hidden="true">→</span></a><button class="button button-secondary" data-action="explore-demo">Explore Demo</button></div><div class="notice"><span class="notice-icon" aria-hidden="true">i</span><div><strong>Educational-use prototype</strong><p class="supporting">This shared prototype uses fictional data and demonstration rules pending fitness-professional review.</p></div></div></section><aside class="welcome-visual" aria-label="Preview of a FITSTART result"><div class="focus-preview"><span class="badge badge-primary">Main Focus</span><p class="eyebrow spacer-top">Maria’s starting point</p><h2>Body-Fat Percentage</h2><div class="metric-value"><strong>29</strong><span>%</span></div><p>This result may deserve attention based on its demo classification and Maria’s stated goal.</p><hr class="divider"><div class="cluster"><span class="badge badge-attention">Demo: Higher Attention</span><span class="badge badge-confirmed">Original value preserved</span></div></div></aside></main></div>`;}

export function profileSetupView(state){const p=state.profile||demoProfile;return `<div class="content">${pageHeader("Step 1 of 3","Create your profile","We only ask for context that helps explain your assessment. No medical history is collected.")}<div class="content-grid"><form class="card profile-form" id="profile-setup-form" novalidate><section class="form-section"><div class="field"><label for="displayName">Display name or nickname</label><input class="input" id="displayName" name="displayName" value="${escapeHtml(state.profile?.displayName||"")}" autocomplete="nickname" required><p class="field-help">Used only to personalize the prototype on this device.</p><p class="field-error" data-error="displayName" aria-live="polite"></p></div></section><section class="form-section"><fieldset class="field"><legend>Primary goal</legend><p class="field-help">Goal relevance affects ordering, but never changes the FitMao classification.</p>${radioGroup("primaryGoal",goals,state.profile?.primaryGoal||"")}<p class="field-error" data-error="primaryGoal" aria-live="polite"></p></fieldset><div class="field"><label for="secondaryGoal">Optional secondary goal</label><select class="select" id="secondaryGoal" name="secondaryGoal">${selectOptions(goals,state.profile?.secondaryGoal||"","No secondary goal")}</select><p class="field-error" data-error="secondaryGoal" aria-live="polite"></p></div></section><section class="form-section"><fieldset class="field"><legend>Fitness experience</legend><p class="field-help">Experience changes explanation depth, not metric importance.</p>${radioGroup("experience",["new","some-experience","experienced"],state.profile?.experience||"")}<p class="field-error" data-error="experience" aria-live="polite"></p></fieldset></section><section class="form-section"><fieldset class="field"><legend>Preferred activities</legend><p class="field-help">Used only to make educational first steps feel relevant.</p>${chips("activities",activities,state.profile?.activities||[])}</fieldset></section><section class="form-section"><div class="field"><label for="availability">Realistic weekly availability</label><select class="select" id="availability" name="availability">${selectOptions(["1-day-week","2-days-week","3-days-week","4-plus-days-week"],state.profile?.availability||"")}</select></div><fieldset class="field"><legend>What do you expect from FITSTART?</legend>${chips("expectations",expectations,state.profile?.expectations||[])}</fieldset><fieldset class="field"><legend>What may make starting difficult?</legend><p class="field-help">Barriers shape Quick Wins, not assessment classification.</p>${chips("barriers",barriers,state.profile?.barriers||[])}</fieldset></section><div class="form-actions"><a class="button button-secondary" href="#/welcome">Back</a><button class="button button-primary" type="submit">Save Profile</button></div></form><aside class="stack-lg"><div class="card card-soft"><p class="eyebrow">Why this matters</p><h3>A clearer explanation, not a new diagnosis</h3><p>Your answers help FITSTART describe why a result may be relevant and which educational next step may feel manageable.</p></div>${educationalNotice()}</aside></div></div>`;}

export function dashboardView(state){const assessment=currentAssessment(state);const result=resultFor(state);const name=escapeHtml(state.profile?.displayName||"Member");const firstUse=!assessment;return `<div class="content">${pageHeader("Member dashboard",`Welcome, ${name}`,firstUse?"Your profile is ready. Add a confirmed assessment to create your personalized starting point.":"Your latest starting point is ready to review.",`<a class="button button-primary" href="#/assessment/import">${firstUse?"Add My FitMao Assessment":"Add New Assessment"}</a>`)}${educationalNotice()}<section class="card profile-hero"><div class="avatar" aria-hidden="true">${name.slice(0,1).toUpperCase()}</div><div><p class="eyebrow">Current main goal</p><h2>${label(state.profile?.primaryGoal)}</h2><p class="supporting">Experience: ${label(state.profile?.experience)} · Availability: ${label(state.profile?.availability)}</p></div><a class="button button-secondary" href="#/profile">Review profile</a></section>${firstUse?`<section class="two-columns results-section"><div class="card empty-state"><div class="empty-icon" aria-hidden="true">＋</div><h2>No assessment yet</h2><p class="muted">Choose demo data, manual entry, a simulated screenshot, or a simulated QR result.</p><a class="button button-primary spacer-top" href="#/assessment/import">Add My FitMao Assessment</a></div><div class="card card-soft"><p class="eyebrow">Learn first</p><h2>Metric Mini-Library</h2><p>Explore plain-language definitions before adding an assessment.</p><a class="button button-soft spacer-top" href="#/learn">Browse the library</a></div></section>`:`<section class="content-grid results-section"><div class="stack-lg"><article class="card main-focus"><div class="main-focus-grid"><div><span class="badge">Main Focus</span><p class="eyebrow spacer-top">Based on demonstration rules</p><h2>${escapeHtml(result.mainFocus?.name||"Insufficient confirmed data")}</h2><p>${result.mainFocus?explainMetric(result.mainFocus,state.profile).why:"Review the confirmed values before generating priorities."}</p></div>${result.mainFocus?`<div><p class="supporting">Original FitMao value</p><div class="metric-value"><strong>${result.mainFocus.value}</strong><span>${escapeHtml(result.mainFocus.unit)}</span></div>${badge(result.mainFocus)}</div>`:""}</div></article><div class="card"><div class="card-header"><div><p class="eyebrow">Top priorities</p><h2>Your next two results</h2></div></div><div class="priority-list">${result.priorities.map((item,index)=>`<div class="priority-card"><span class="priority-rank">${index+2}</span><div><strong>${escapeHtml(item.name)} · ${item.value} ${escapeHtml(item.unit)}</strong><p class="supporting">${explainMetric(item,state.profile).why}</p></div></div>`).join("")||"<p class='muted'>No additional eligible priorities in this demo.</p>"}</div><a class="button button-primary spacer-top" href="#/results">View Results</a></div></div><aside class="stack-lg"><div class="card"><p class="eyebrow">Latest assessment</p><h3>${formatDate(assessment.assessedAt)}</h3><p class="supporting">${assessment.metrics.length} reviewed measurements · Confirmed</p></div>${state.assessments.length<2?`<div class="card"><span class="badge badge-neutral">Locked</span><h3 class="spacer-top">Compare measurements</h3><p>Available after your next confirmed assessment.</p><button class="button button-secondary spacer-top" disabled>Compare Measurements</button></div>`:`<div class="card card-accent"><p class="eyebrow">Two confirmed assessments</p><h3>Compare measured values</h3><p>See only your latest and immediately previous assessment.</p><a class="button button-secondary spacer-top" href="#/compare">Compare Measurements</a></div>`}<div class="card card-soft"><h3>Metric Mini-Library</h3><p>Learn what each measurement generally represents.</p><a class="text-link" href="#/learn">Browse the library →</a></div></aside></section>`}</div>`;}

export function assessmentImportView(){return `<div class="content">${pageHeader("Assessment capture","Add your FitMao assessment","Choose a guided method. Every captured value must be reviewed before demonstration scoring begins.")} ${notice("Screenshot and QR extraction are simulated in this frontend prototype. All assessment values must be reviewed before results are generated.","warning","Simulation notice")}<section class="two-columns"><article class="card method-card"><div class="method-icon" aria-hidden="true">D</div><h2>Use Demo Assessment</h2><p class="muted">Load Maria’s complete fictional FitMao-style assessment.</p><button class="button button-primary" data-action="use-demo-assessment">Use Demo Assessment</button></article><article class="card method-card"><div class="method-icon" aria-hidden="true">▣</div><h2>Upload Report Screenshot</h2><p class="muted">Choose an image for temporary preview. Image bytes are never saved to local storage.</p><div class="field spacer-top"><label for="report-upload" class="sr-only">Choose report screenshot</label><input class="input" id="report-upload" type="file" accept="image/*"></div><button class="button button-secondary" data-action="simulate-screenshot">Simulate Extraction</button></article><article class="card method-card"><div class="method-icon" aria-hidden="true">⌗</div><h2>Use QR Result</h2><p class="muted">Preview the QR handoff. Camera scanning and FitMao connectivity are not implemented.</p><button class="button button-secondary" data-action="simulate-qr">Simulate QR Result</button></article></section><div class="form-actions spacer-top"><a class="button button-secondary" href="#/dashboard">Back to Dashboard</a></div></div>`;}

export function assessmentReviewView(state){const assessment=state.draftAssessment; if(!assessment)return emptyRecovery("No draft assessment","Choose an input method before reviewing values.","#/assessment/import","Choose input method");const previous=currentAssessment(state);const raw=assessment.metrics.map(m=>`<div class="raw-row"><span>${escapeHtml(m.name)}</span><strong>${m.value===""?"—":escapeHtml(m.value)} ${escapeHtml(m.unit)}</strong></div>`).join("");return `<div class="content">${pageHeader("Step 2 of 3","Review captured values","Check every value against the original report. You can edit, mark unavailable, or reset each row.")}${assessment.sourceType!=="manual"?notice(`${assessment.sourceType==="demo"?"Demo values":"Imported values"} are simulated and require confirmation.`,"warning","Review required"):""}<form id="assessment-review-form" novalidate><div class="content-grid"><section class="stack"><div class="table-wrap"><table class="data-table responsive-table"><thead><tr><th>Metric</th><th>Captured Value</th><th>Unit</th><th>FitMao Category</th><th>Edit</th></tr></thead><tbody>${assessment.metrics.map(metric=>`<tr><td data-label="Metric"><strong>${escapeHtml(metric.name)}</strong><p class="field-error" data-error="${metric.id}" aria-live="polite"></p></td><td data-label="Captured value"><input class="input" name="metric-${metric.id}" type="number" step="any" value="${escapeHtml(metric.value)}" ${metric.value===null?"disabled":""} aria-label="${escapeHtml(metric.name)} value"></td><td data-label="Unit">${escapeHtml(metric.unit)}</td><td data-label="Category"><select class="select" name="category-${metric.id}" aria-label="${escapeHtml(metric.name)} demo category"><option value="within" ${metric.sourceCategory==="within"?"selected":""}>Demo: Within Reference Range</option><option value="attention" ${metric.sourceCategory==="attention"?"selected":""}>Demo: Needs Review</option><option value="higher-attention" ${metric.sourceCategory==="higher-attention"?"selected":""}>Demo: Higher Attention</option></select></td><td data-label="Edit"><div class="cluster"><button class="text-link" type="button" data-action="toggle-unavailable" data-id="${metric.id}">${metric.value===null?"Restore":"Mark unavailable"}</button><button class="text-link" type="button" data-action="reset-metric" data-id="${metric.id}">Reset</button></div></td></tr>`).join("")}</tbody></table></div>${previous?notice("FITSTART will flag unusually large differences after confirmation so both reports can be verified.","warning","Previous assessment detected"):""}<label class="notice"><input type="checkbox" name="reviewed" required><span><strong>I reviewed these values against my FitMao report.</strong><span class="supporting"> Confirmation is required before scoring begins.</span></span></label><p class="field-error" data-error="reviewed" aria-live="polite"></p><div class="form-actions"><a class="button button-secondary" href="#/assessment/import">Change method</a><button class="button button-primary" type="submit">Confirm Assessment</button></div></section><aside class="report-preview-sticky"><div class="raw-report"><div class="raw-report-header"><strong>FITMAO-STYLE DEMO REPORT</strong><p>Fictional values · ${escapeHtml(assessment.sourceType)}</p></div>${raw}<p class="supporting spacer-top">Preview only. No professional verification or live FitMao integration.</p></div></aside></div></form></div>`;}

const surveyQuestions=[
  {title:"What is your main goal right now?",why:"Goal relevance can affect priority order, but never changes the original FitMao classification.",content:p=>`<fieldset class="field"><legend class="sr-only">Primary goal</legend>${radioGroup("primaryGoal",goals,p.primaryGoal)}</fieldset><div class="field spacer-top"><label for="secondaryGoal">Optional secondary goal</label><select class="select" name="secondaryGoal" id="secondaryGoal">${selectOptions(goals,p.secondaryGoal,"No secondary goal")}</select></div>`},
  {title:"How familiar are you with fitness and assessment terms?",why:"Experience changes explanation depth, not the physical importance of any metric.",content:p=>`<fieldset class="field"><legend class="sr-only">Experience</legend>${radioGroup("experience",["new","some-experience","experienced"],p.experience)}</fieldset><div class="field spacer-top"><label for="confidence">Confidence understanding assessment terms</label><select class="select" name="confidence" id="confidence">${selectOptions(["low","medium","high"],p.confidence)}</select></div>`},
  {title:"Which activities feel realistic or interesting to you?",why:"Preferences are used only as contextual educational language for first steps.",content:p=>`<fieldset class="field"><legend class="sr-only">Preferred activities</legend>${chips("activities",activities,p.activities)}</fieldset>`},
  {title:"How many days feel realistically available each week?",why:"Availability shapes Quick Wins; it does not change assessment classification or importance.",content:p=>`<fieldset class="field"><legend class="sr-only">Weekly availability</legend>${radioGroup("availability",["1-day-week","2-days-week","3-days-week","4-plus-days-week"],p.availability)}</fieldset>`},
  {title:"What do you expect, and what may make starting difficult?",why:"These answers help FITSTART keep next steps simple, relevant, and educational.",content:p=>`<fieldset class="field"><legend>Expectations</legend>${chips("expectations",expectations,p.expectations)}</fieldset><fieldset class="field spacer-top"><legend>Barriers</legend>${chips("barriers",barriers,p.barriers)}</fieldset>`}
];
export function surveyView(state,rawStep){if(!currentAssessment(state))return emptyRecovery("Confirm an assessment first","The survey begins after assessment review.","#/assessment/import","Add assessment");const step=Math.min(5,Math.max(1,Number(rawStep)||1));const p={...state.profile,...state.draftSurvey};const q=surveyQuestions[step-1];return `<div class="content"><div class="reading-column"><p class="progress-label">Step ${step} of 5 · Contextual survey</p><div class="stepper" aria-label="Survey progress">${[1,2,3,4,5].map(i=>`<span class="step ${i<step?"is-complete":i===step?"is-current":""}"></span>`).join("")}</div><form class="card question-card" id="survey-form" data-step="${step}"><p class="eyebrow">Tell us about you</p><h1>${escapeHtml(q.title)}</h1><details class="accordion spacer-top"><summary>Why this question matters</summary><div class="accordion-body supporting">${escapeHtml(q.why)}</div></details><div class="spacer-top">${q.content(p)}</div><p class="field-error spacer-top" data-error="survey" aria-live="polite"></p><div class="form-actions spacer-top">${step===1?'<a class="button button-secondary" href="#/assessment/review">Back</a>':`<a class="button button-secondary" href="#/survey/${step-1}">Back</a>`}<button class="button button-primary" type="submit">${step===5?"Review Answers":"Continue"}</button></div></form><p class="supporting spacer-top">Draft answers are saved locally on this device.</p></div></div>`;}

export function confirmView(state){const p={...state.profile,...state.draftSurvey};const changed=state.profile?.primaryGoal&&p.primaryGoal!==state.profile.primaryGoal;return `<div class="content">${pageHeader("Step 3 of 3","Review and confirm","Give every input one final check before demonstration rules organize the results.")}${changed?notice(`Your main goal changed from ${label(state.profile.primaryGoal)} to ${label(p.primaryGoal)}. FITSTART will recalculate your priorities using your new goal.`,"warning","Goal Update Notice"):""}<div class="content-grid"><section class="stack-lg"><div class="card"><div class="card-header"><div><span class="badge badge-confirmed">Assessment confirmed</span><h2 class="spacer-top">${formatDate(currentAssessment(state)?.assessedAt)}</h2></div><a class="button button-secondary" href="#/assessment/review">Edit Assessment</a></div><p>${currentAssessment(state)?.metrics.filter(m=>m.value!==null).length} available measurements were reviewed. Original values remain visible in every result.</p></div><div class="card"><div class="card-header"><h2>Your context</h2><a class="button button-secondary" href="#/survey/1">Edit Profile Answers</a></div><dl class="summary-list"><div class="summary-row"><dt>Primary goal</dt><dd>${label(p.primaryGoal)}</dd></div><div class="summary-row"><dt>Secondary goal</dt><dd>${label(p.secondaryGoal)||"None"}</dd></div><div class="summary-row"><dt>Experience</dt><dd>${label(p.experience)}</dd></div><div class="summary-row"><dt>Activities</dt><dd>${p.activities?.map(label).join(", ")||"None selected"}</dd></div><div class="summary-row"><dt>Availability</dt><dd>${label(p.availability)}</dd></div><div class="summary-row"><dt>Barriers</dt><dd>${p.barriers?.map(label).join(", ")||"None selected"}</dd></div></dl></div><button class="button button-primary button-block" data-action="confirm-and-generate">Confirm and Generate My Starting Point</button></section><aside class="stack-lg">${educationalNotice()}<div class="card card-accent"><p class="eyebrow">Transparent process</p><h3>What happens next</h3><p>FITSTART checks confirmed values, matches approved demo goal relationships, ranks priorities, and prepares plain-language explanations. No AI or medical diagnosis is used.</p></div></aside></div></div>`;}

export function processingView(){return `<div class="content"><div class="reading-column"><p class="eyebrow">Preparing your result</p><h1>Creating your Personalized Starting Point</h1><p class="muted">This brief sequence shows exactly what the demonstration rules are doing.</p><div class="processing-list" aria-live="polite"><div class="processing-step is-active" data-process-step="0"><span class="processing-dot"></span><strong>Checking confirmed assessment values</strong></div><div class="processing-step" data-process-step="1"><span class="processing-dot"></span><strong>Matching metrics with your goal</strong></div><div class="processing-step" data-process-step="2"><span class="processing-dot"></span><strong>Organizing priorities</strong></div><div class="processing-step" data-process-step="3"><span class="processing-dot"></span><strong>Preparing plain-language explanations</strong></div></div><p class="supporting">Prototype scoring for research demonstration; pending professional validation.</p></div></div>`;}

export function resultsView(state){const assessment=currentAssessment(state);if(!assessment)return emptyRecovery("No confirmed result","Add and confirm an assessment to generate a starting point.","#/assessment/import","Add assessment");const result=resultFor(state);if(result.insufficient)return `<div class="content">${pageHeader("Personalized Starting Point","More information is needed","FITSTART could not create an honest priority from the available confirmed values.")}${notice("Review missing or unavailable measurements. Missing metrics are excluded and never silently scored as zero.","warning","Insufficient data")}<a class="button button-primary" href="#/assessment/review">Review inputs</a></div>`;const main=result.mainFocus,exp=explainMetric(main,state.profile),wins=quickWins(state.profile);return `<div class="content">${pageHeader("Personalized Starting Point","Start with what matters to you",`Based on Maria’s confirmed fictional assessment from ${formatDate(assessment.assessedAt)}.`,`<a class="button button-secondary" href="#/research-comparison">Research comparison</a>`)}${educationalNotice()}<article class="card main-focus"><div class="main-focus-grid"><div><span class="badge">Main Focus</span><p class="eyebrow spacer-top">Your clearest starting point</p><h2>${escapeHtml(main.name)}</h2><p>${escapeHtml(exp.meaning)}</p><p class="spacer-top"><strong>${escapeHtml(exp.why)}</strong></p><a class="button button-secondary spacer-top" href="#/results/metric/${main.id}">See Why</a></div><div><p class="supporting">Original FitMao value</p><div class="metric-value"><strong>${main.value}</strong><span>${escapeHtml(main.unit)}</span></div>${badge(main)}</div></div></article><section class="results-section"><div class="section-heading"><p class="eyebrow">Why this was selected</p><h2>Traceable to what you shared</h2></div><div class="why-grid"><div class="why-callout"><strong>You told us</strong><p>${escapeHtml(exp.toldUs)}</p></div><div class="why-callout"><strong>Your assessment showed</strong><p>${escapeHtml(exp.showed)}</p></div></div></section><section class="results-section"><div class="section-heading"><p class="eyebrow">Top priorities</p><h2>Other results worth understanding</h2></div><div class="priority-list">${result.priorities.map((item,index)=>`<article class="card priority-card"><span class="priority-rank">${index+2}</span><div><h3>${escapeHtml(item.name)}</h3><p><strong>Original value: ${item.value} ${escapeHtml(item.unit)}</strong> · ${escapeHtml(item.sourceCategoryLabel)}</p><p class="supporting">${escapeHtml(explainMetric(item,state.profile).why)}</p></div><a class="button button-secondary" href="#/results/metric/${item.id}">Full breakdown</a></article>`).join("")}</div></section><section class="results-section two-columns"><div class="card"><p class="eyebrow">How your priorities connect</p><h2>Consider these together</h2><div class="connection-line spacer-top"><strong>${escapeHtml(main.name)}</strong><span class="connection-link" aria-hidden="true">↔</span><strong>${escapeHtml(result.priorities[0]?.name||"Related measurements")}</strong></div><p class="supporting spacer-top">These measurements can be considered together based only on the research-team-approved demo relationship map. This does not mean one caused the other.</p></div><div class="card card-accent"><p class="eyebrow">First step</p><h2>Understand one priority</h2><p>Review the full breakdown for ${escapeHtml(main.name)} and write down one question to discuss with a qualified fitness professional.</p><a class="button button-secondary spacer-top" href="#/results/metric/${main.id}">Open the breakdown</a></div></section><section class="results-section content-grid"><div class="card"><p class="eyebrow">Quick Wins</p><h2>Small educational next steps</h2><div class="stack spacer-top">${wins.map(win=>`<div class="quick-win"><p>${escapeHtml(win)}</p></div>`).join("")}</div></div><aside class="card card-soft"><p class="eyebrow">Explore for you</p><h2>Learn the terms</h2><p>Read definitions, general relevance, and measurement limitations for your priority metrics.</p><a class="button button-soft spacer-top" href="#/learn">Explore the Metric Library</a></aside></section><section class="results-section two-columns"><div class="card"><p class="eyebrow">Unprocessed assessment</p><h2>View every original value</h2><p>Nothing in the report is replaced or hidden by the personalized presentation.</p><button class="button button-secondary spacer-top" data-action="view-full-assessment">View Full FitMao Assessment</button></div><div class="notice notice-warning"><span class="notice-icon" aria-hidden="true">!</span><div><strong>Professional guidance stays visible</strong><p>Consider discussing this result with a qualified fitness professional, especially before making changes based on any measurement.</p></div></div></section></div>`;}

export function metricDetailView(state,id,fromLibrary=false){const definition=getMetricDefinition(id);if(!definition)return notFoundView();const assessment=currentAssessment(state);const scored=resultFor(state).items.find(item=>item.id===id);if(!scored||fromLibrary)return learnMetricView(state,id);const exp=explainMetric(scored,state.profile);return `<div class="content"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="#/results">Results</a><span>/</span><span>${escapeHtml(scored.name)}</span></nav>${pageHeader("Priority detail",scored.name,"See the original value, the inputs that mattered, and the demonstration score breakdown.")}<div class="content-grid"><section class="stack-lg"><div class="card"><div class="card-header"><div>${badge(scored)}<h2 class="spacer-top">Original FitMao value</h2></div><div class="metric-value"><strong>${scored.value}</strong><span>${escapeHtml(scored.unit)}</span></div></div><p>${escapeHtml(definition.definition)}</p><p class="supporting spacer-top">${escapeHtml(definition.limitation)}</p></div><div class="why-grid"><div class="why-callout"><strong>You told us</strong><p>${escapeHtml(exp.toldUs)}</p></div><div class="why-callout"><strong>Your assessment showed</strong><p>${escapeHtml(exp.showed)}</p></div></div><div class="card"><h2>Why it was prioritized</h2><p>${escapeHtml(exp.why)}</p><details class="accordion spacer-top"><summary>View technical demonstration score</summary><div class="accordion-body"><p class="supporting">Prototype scoring for research demonstration; pending professional validation.</p><dl class="summary-list"><div class="summary-row"><dt>Assessment status</dt><dd>+${scored.breakdown.status} points</dd></div><div class="summary-row"><dt>Primary goal relevance</dt><dd>+${scored.breakdown.primary} points</dd></div><div class="summary-row"><dt>Secondary goal relevance</dt><dd>+${scored.breakdown.secondary} points</dd></div><div class="summary-row"><dt>Approved relationship</dt><dd>+${scored.breakdown.relationship} points</dd></div><div class="summary-row"><dt>Final mock score</dt><dd>${scored.score} · ${escapeHtml(scored.band)}</dd></div></dl></div></details></div></section><aside class="stack-lg"><div class="card card-soft"><p class="eyebrow">Related metrics</p>${definition.related.map(rel=>{const m=getMetricDefinition(rel);return `<a class="text-link" href="#/learn/${m.id}">${escapeHtml(m.name)} →</a>`}).join("<br>")}</div>${notice("Consider discussing this result with a qualified fitness professional. FITSTART does not provide diagnosis or treatment.","warning","Professional-guidance note")}</aside></div></div>`;}

export function compareView(state){if(state.assessments.length<2)return emptyRecovery("Comparison is not available yet","Available after your next confirmed assessment. FITSTART compares only the latest and immediately previous assessment.","#/assessment/import","Add another assessment");const [previous,current]=state.assessments.slice(-2);const rows=compareAssessments(previous,current,state.profile.primaryGoal);const anyLarge=rows.some(row=>row.large);const days=Math.round((new Date(current.assessedAt)-new Date(previous.assessedAt))/86400000);return `<div class="content">${pageHeader("Two-assessment comparison","Latest Assessment compared with Previous Assessment",`${formatDate(previous.assessedAt)} to ${formatDate(current.assessedAt)} · ${days} days apart`)}${notice("Measurements can vary with hydration, timing, device method, and other conditions. This page reports measured differences only; it does not prove progress or improvement.","warning","Measurement-variability reminder")}${anyLarge?notice("One or more measured differences exceed a configurable demonstration threshold. Verify both reports before interpretation.","warning","Large difference detected — verify both reports"):""}<div class="table-wrap"><table class="data-table responsive-table"><thead><tr><th>Metric</th><th>Previous</th><th>Current</th><th>Measured change</th><th>Goal connection</th></tr></thead><tbody>${rows.map(row=>{const desc=row.difference===0?"No measured change":`${row.direction==="up"?"Increased":"Decreased"} by ${Math.abs(Number(row.difference.toFixed(2)))} ${escapeHtml(row.unit)}`;return `<tr><td data-label="Metric"><strong>${escapeHtml(row.name)}</strong>${row.large?'<p class="supporting">⚠ Verify both reports</p>':""}</td><td data-label="Previous">${row.previousValue} ${escapeHtml(row.unit)}</td><td data-label="Current">${row.currentValue} ${escapeHtml(row.unit)}</td><td data-label="Measured change"><span class="difference-${row.direction}">${desc}</span></td><td data-label="Goal connection">${escapeHtml(row.goalConnection)}</td></tr>`}).join("")}</tbody></table></div><div class="form-actions spacer-top"><a class="button button-secondary" href="#/dashboard">Back to Dashboard</a><a class="button button-primary" href="#/results">View Latest Results</a></div></div>`;}

export function learnView(state){const recent=state.ui.recentMetrics||[];return `<div class="content">${pageHeader("Metric Mini-Library","Learn what the numbers mean","Plain-language definitions, general relevance, and important interpretation limitations—available with or without a result.")}<div class="search-row"><label class="sr-only" for="metric-search">Search metrics</label><input class="input" type="search" id="metric-search" placeholder="Search by metric name or abbreviation…"><button class="button button-secondary" data-action="clear-search">Clear</button></div><div class="filter-row spacer-top" aria-label="Metric categories"><button class="filter is-active" data-filter="all">All metrics</button>${[...new Set(metricLibrary.map(m=>m.category))].map(cat=>`<button class="filter" data-filter="${cat}">${cat}</button>`).join("")}</div>${recent.length?`<p class="supporting spacer-top">Recently viewed: ${recent.map(id=>`<a href="#/learn/${id}">${escapeHtml(getMetricDefinition(id)?.name||id)}</a>`).join(" · ")}</p>`:""}<section class="three-columns results-section" id="library-grid">${metricLibrary.slice().sort((a,b)=>a.name.localeCompare(b.name)).map(metric=>`<article class="card library-card" data-library-card data-name="${escapeHtml((metric.name+" "+metric.abbr).toLowerCase())}" data-category="${metric.category}"><span class="badge badge-neutral">${escapeHtml(metric.category)}</span><h2 class="spacer-top">${escapeHtml(metric.name)}</h2><p class="supporting">${metric.abbr!=="—"?escapeHtml(metric.abbr)+" · ":""}${escapeHtml(metric.definition)}</p><a class="text-link" href="#/learn/${metric.id}">Learn about this metric →</a></article>`).join("")}</section><div class="card empty-state" id="library-empty" hidden><div class="empty-icon" aria-hidden="true">?</div><h2>No matching metric</h2><p>Try a broader search or another category.</p></div></div>`;}

export function learnMetricView(state,id){const metric=getMetricDefinition(id);if(!metric)return notFoundView();const assessmentMetric=currentAssessment(state)?.metrics.find(m=>m.id===id);return `<div class="content"><nav class="breadcrumbs" aria-label="Breadcrumb"><a href="#/learn">Metric Library</a><span>/</span><span>${escapeHtml(metric.name)}</span></nav>${pageHeader(metric.category,metric.name,metric.abbr!=="—"?`Also shown as ${metric.abbr}.`:"A plain-language guide to this measurement.")}<div class="content-grid"><article class="stack-lg"><section class="card"><p class="eyebrow">Plain-language definition</p><h2>${escapeHtml(metric.definition)}</h2></section><section class="card"><h2>What it generally represents</h2><p>${escapeHtml(metric.represents)}</p><h3 class="spacer-top">Why it may be relevant</h3><p>${escapeHtml(metric.relevance)}</p></section><section class="notice notice-warning"><span class="notice-icon" aria-hidden="true">!</span><div><strong>Measurement and interpretation limitation</strong><p>${escapeHtml(metric.limitation)}</p></div></section><section class="card"><p class="eyebrow">Sources</p><h2>Research-team review pending</h2><p class="muted">Approved references will be added here after the thesis team confirms metric definitions and sources.</p></section></article><aside class="stack-lg">${assessmentMetric?`<div class="card card-soft"><p class="eyebrow">Your fictional assessment</p><h3>${assessmentMetric.value} ${escapeHtml(assessmentMetric.unit)}</h3>${badge(assessmentMetric)}<a class="button button-soft button-block spacer-top" href="#/results/metric/${id}">Back to your result</a></div>`:""}<div class="card"><p class="eyebrow">Related metrics</p>${metric.related.map(rel=>{const m=getMetricDefinition(rel);return `<a class="text-link" href="#/learn/${m.id}">${escapeHtml(m.name)} →</a>`}).join("<br>")}</div></aside></div></div>`;}

export function profileView(state){const p=state.profile;if(!p)return emptyRecovery("No profile saved","Create a lightweight profile before using FITSTART.","#/profile/setup","Create profile");return `<div class="content">${pageHeader("Member profile","Your fitness context","Update deliberately. A changed main goal always triggers a Goal Update Notice.")}<div class="content-grid"><form class="card profile-form" id="profile-update-form"><div class="field"><label for="displayName">Display name</label><input class="input" id="displayName" name="displayName" value="${escapeHtml(p.displayName)}"></div><fieldset class="field"><legend>Primary goal</legend>${radioGroup("primaryGoal",goals,p.primaryGoal)}</fieldset><div class="field"><label for="secondaryGoal">Secondary goal</label><select class="select" id="secondaryGoal" name="secondaryGoal">${selectOptions(goals,p.secondaryGoal,"No secondary goal")}</select></div><fieldset class="field"><legend>Experience</legend>${radioGroup("experience",["new","some-experience","experienced"],p.experience)}</fieldset><fieldset class="field"><legend>Activities</legend>${chips("activities",activities,p.activities)}</fieldset><div class="field"><label for="availability">Availability</label><select class="select" id="availability" name="availability">${selectOptions(["1-day-week","2-days-week","3-days-week","4-plus-days-week"],p.availability)}</select></div><fieldset class="field"><legend>Expectations</legend>${chips("expectations",expectations,p.expectations)}</fieldset><fieldset class="field"><legend>Barriers</legend>${chips("barriers",barriers,p.barriers)}</fieldset><p class="field-error" data-error="profile" aria-live="polite"></p><button class="button button-primary" type="submit">Save Profile Changes</button></form><aside class="stack-lg"><div class="card card-accent"><p class="eyebrow">Local demo data</p><h2>Stored on this device</h2><p>This fictional profile and up to two assessments are saved under one versioned browser-storage key.</p><button class="button button-danger spacer-top" data-action="clear-data">Clear Demo Data</button></div>${educationalNotice()}<div class="card"><p class="eyebrow">Research view</p><h3>Original Report vs FITSTART</h3><p>Use the same fictional assessment in both neutral conditions.</p><a class="text-link" href="#/research-comparison">Open research comparison →</a></div></aside></div></div>`;}

export function researchView(state){const assessment=currentAssessment(state)||demoAssessments[1];const result=scoreAssessment(assessment,state.profile||demoProfile);return `<div class="content">${pageHeader("Thesis evaluation","Original Report and FITSTART Presentation","Both conditions use identical fictional source values. This interface enables comparison; it does not prove effectiveness.")}<div class="tab-list desktop-hide" role="tablist" aria-label="Research conditions"><button class="tab" role="tab" aria-selected="true" data-research-tab="original">Original Report</button><button class="tab" role="tab" aria-selected="false" data-research-tab="fitstart">FITSTART Presentation</button></div><div class="two-columns tablet-tabs spacer-top"><section class="card research-panel is-active" data-research-panel="original"><div class="research-panel-title"><p class="eyebrow">Condition A</p><h2>Original Report</h2></div><div class="raw-report"><div class="raw-report-header"><strong>FITMAO-STYLE DEMO REPORT</strong><p>${formatDate(assessment.assessedAt)}</p></div>${assessment.metrics.map(m=>`<div class="raw-row"><span>${escapeHtml(m.name)}</span><strong>${m.value} ${escapeHtml(m.unit)} · ${escapeHtml(m.sourceCategoryLabel)}</strong></div>`).join("")}</div></section><section class="card research-panel" data-research-panel="fitstart"><div class="research-panel-title"><p class="eyebrow">Condition B</p><h2>FITSTART Presentation</h2></div><span class="badge badge-primary">Main Focus</span><h3 class="spacer-top">${escapeHtml(result.mainFocus?.name)}</h3><div class="metric-value"><strong>${result.mainFocus?.value}</strong><span>${escapeHtml(result.mainFocus?.unit)}</span></div><p>${escapeHtml(explainMetric(result.mainFocus,state.profile||demoProfile).why)}</p><hr class="divider"><p class="eyebrow">Prioritized order</p>${result.priorities.map((m,i)=>`<p class="spacer-top"><strong>${i+2}. ${escapeHtml(m.name)} · ${m.value} ${escapeHtml(m.unit)}</strong><br><span class="supporting">${escapeHtml(explainMetric(m,state.profile||demoProfile).meaning)}</span></p>`).join("")}<div class="notice notice-warning spacer-top"><span class="notice-icon" aria-hidden="true">!</span><div><strong>Professional-guidance reminder</strong><p class="supporting">Discuss results with a qualified fitness professional.</p></div></div></section></div><section class="card results-section"><div class="card-header"><div><p class="eyebrow">Research controls</p><h2>Comprehension questions</h2></div><button class="button button-secondary" data-action="begin-comprehension">Begin comprehension questions</button></div><div id="comprehension" hidden><fieldset class="research-question"><legend><strong>Which measurement was presented as the Main Focus?</strong></legend>${radioGroup("research-main-focus",[result.mainFocus?.id,"skeletal-muscle-mass","body-water"].filter(Boolean),state.ui.research?.answers?.mainFocus)}</fieldset><fieldset class="research-question"><legend><strong>Did FITSTART replace the original FitMao value?</strong></legend><div class="choice-grid"><div class="choice"><input type="radio" id="replace-no" name="research-replace" value="no"><label for="replace-no">No, it stayed visible</label></div><div class="choice"><input type="radio" id="replace-yes" name="research-replace" value="yes"><label for="replace-yes">Yes, it changed</label></div></div></fieldset><button class="button button-primary spacer-top" data-action="save-comprehension">Save responses locally</button></div><p class="supporting spacer-top">The prototype may record which condition was viewed first, time spent, comprehension responses, preference, and perceived usefulness. Evaluation analysis belongs to the research methodology.</p></section></div>`;}

export function notFoundView(){return `<div class="content"><div class="card empty-state"><div class="empty-icon" aria-hidden="true">?</div><p class="eyebrow">Page not found</p><h1>This route is not part of the prototype.</h1><p class="muted">Return to the dashboard to continue with the fictional Maria scenario.</p><a class="button button-primary spacer-top" href="#/dashboard">Go to Dashboard</a></div></div>`;}
function emptyRecovery(title,text,href,action){return `<div class="content"><div class="card empty-state"><div class="empty-icon" aria-hidden="true">i</div><h1>${escapeHtml(title)}</h1><p class="muted">${escapeHtml(text)}</p><a class="button button-primary spacer-top" href="${href}">${escapeHtml(action)}</a></div></div>`;}

export { blankMetrics, demoAssessments, rules };
